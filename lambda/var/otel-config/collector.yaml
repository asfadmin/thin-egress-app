receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  logging:
    loglevel: debug

  otlphttp/otelcollector:
    endpoint: "https://metrics-otel.ngap.uat.earthdata.nasa.gov"


#enables output for traces to log
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/otelcollector,logging]